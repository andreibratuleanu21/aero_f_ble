name: aero_f_ble_example
description: Demonstrates how to use the aero_f_ble plugin.
version: 1.0.0+1

publish_to: 'none'

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  aero_f_ble:
    path: ../
  cupertino_icons: ^1.0.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true
