name: aero_f_ble
description: Aero Flutter BLE library.
version: 0.0.1
homepage: https://github.com/andreibratuleanu21/aero_f_ble

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: net.bytex.aero_f_ble
        pluginClass: AeroFBlePlugin
      ios:
        pluginClass: AeroFBlePlugin
